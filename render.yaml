services:
  # A Docker web service
  - type: web
    name: proxylite
    env: docker
    repo: proxylite/proxyservice  # optional
    region: oregon # optional (defaults to oregon)
    plan: free # optional (defaults to starter)
    healthCheckPath: /
    envVars:
      - key: USER_ID
        #value: 494374
